name: Deploy to Onamae Rental Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        uses: actions/setup-node@v4.0.1
        with:
          node-version: 20
      - run: |
          npm install
          npm run build

      - name: Set CMS environment variables
        run: |
          echo "SERVICE_DOMAIN=${{ secrets.CMS_SERVICE_DOMAIN }}" >> $GITHUB_ENV
          echo "API_KEY=${{ secrets.CMS_API_KEY }}" >> $GITHUB_ENV

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.FTP_TARGET_DIR }}/
          local-dir: dist/
          protocol: ftp
